language: java
install: true
dist: trusty
jdk:
  - openjdk13
branches:
  only:
  - master
script:
  - ./gradlew --continue clean build --scan -s -p modules/ingest-geoip/
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_API_KEY